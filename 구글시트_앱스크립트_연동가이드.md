# 구글시트 + Apps Script 연동 가이드

이 문서는 현재 랜딩페이지의 상담문의 데이터가 Google Sheets에 자동 저장되도록 연결하는 방법을 설명합니다.

대상 파일:

- 랜딩페이지: [index.html](C:/land/260701-land/index.html)
- 서버: [server.js](C:/land/260701-land/server.js)
- Apps Script 코드: [google-apps-script/Code.gs](C:/land/260701-land/google-apps-script/Code.gs)

## 1. 현재 동작 구조

현재 랜딩페이지는 아래 순서로 동작합니다.

1. 사용자가 상담문의 폼을 제출하거나 전화상담 버튼을 클릭합니다.
2. 브라우저가 `/api/leads`로 고객 정보를 전송합니다.
3. `server.js`가 데이터를 `data/leads.ndjson`에 저장합니다.
4. `GOOGLE_SCRIPT_URL`이 설정되어 있으면 같은 데이터를 Google Apps Script 웹앱으로 한 번 더 전송합니다.
5. Apps Script가 연결된 Google Spreadsheet의 `Leads` 시트에 한 줄씩 추가합니다.

즉, 로컬 저장과 Google Sheets 저장이 함께 되는 구조입니다.

## 2. 구글 스프레드시트 만들기

1. Google Drive에 로그인합니다.
2. 새 Google 스프레드시트를 하나 생성합니다.
3. 파일명을 예를 들어 `메디스파크 상담DB`로 지정합니다.
4. 시트 이름은 비워둬도 되지만, Apps Script가 자동으로 `Leads` 시트를 생성하거나 기존 `Leads` 시트를 사용합니다.

## 3. Apps Script 만들기

1. 방금 만든 Google Spreadsheet를 엽니다.
2. 상단 메뉴에서 `확장 프로그램 > Apps Script`를 클릭합니다.
3. 기본으로 생성된 `Code.gs` 내용을 모두 지웁니다.
4. 현재 프로젝트의 [google-apps-script/Code.gs](C:/land/260701-land/google-apps-script/Code.gs) 내용을 그대로 붙여넣습니다.
5. 저장합니다.

## 4. Apps Script 배포하기

1. Apps Script 우측 상단의 `배포` 버튼을 클릭합니다.
2. `새 배포`를 선택합니다.
3. 배포 유형은 `웹 앱`을 선택합니다.
4. 설명은 예: `메디스파크 랜딩 리드 수집 API`로 입력합니다.
5. 실행 사용자는 `나`로 설정합니다.
6. 접근 권한은 `모든 사용자`로 설정합니다.
   이 부분이 중요합니다. 랜딩페이지 서버가 외부에서 POST를 보내야 하므로 공개 웹앱이어야 합니다.
7. `배포`를 누릅니다.
8. 권한 승인 창이 나오면 승인합니다.
9. 배포가 끝나면 웹앱 URL을 복사합니다.

예시:

```text
https://script.google.com/macros/s/AKfycbxxxxxxxxxxxxxxxxxxxx/exec
```

## 5. 서버에 Apps Script URL 연결하기

현재 서버는 환경변수 `GOOGLE_SCRIPT_URL`을 읽도록 만들어져 있습니다.

실행 방법 1: PowerShell에서 환경변수를 넣고 실행

```powershell
$env:PORT='4174'
$env:GOOGLE_SCRIPT_URL='https://script.google.com/macros/s/AKfycbxxxxxxxxxxxxxxxxxxxx/exec'
C:\Users\User\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe server.js
```

실행 방법 2: 배포 서버의 환경변수에 등록

- `GOOGLE_SCRIPT_URL`에 웹앱 URL을 넣습니다.
- 서버를 재시작합니다.

## 6. 로컬에서 테스트하는 방법

1. 위처럼 `GOOGLE_SCRIPT_URL`을 넣고 `server.js`를 실행합니다.
2. 브라우저에서 `http://127.0.0.1:4174/index.html`를 엽니다.
3. 상담문의 폼을 제출합니다.
4. Google Spreadsheet로 돌아가서 `Leads` 시트에 새 행이 추가되었는지 확인합니다.

## 7. 저장되는 컬럼

Apps Script는 아래 순서로 데이터를 저장합니다.

1. `created_at`
2. `lead_kind`
3. `name`
4. `phone`
5. `type`
6. `time`
7. `message`
8. `utm_source`
9. `utm_medium`
10. `utm_campaign`
11. `utm_content`
12. `utm_term`
13. `landing_path`
14. `submitted_at_client`
15. `ip`
16. `user_agent`

`lead_kind` 값 예시:

- `inquiry`: 일반 상담문의
- `phone_consultation`: 전화상담 버튼에서 남긴 리드
- `hero_phone_consultation`, `cta_phone_consultation`, `floating_phone_consultation`: 버튼 위치별 전화상담 리드

## 8. 전화상담 버튼 동작

현재 전화상담 버튼은 바로 전화를 걸지 않고 아래 순서로 동작합니다.

1. 전화상담 버튼 클릭
2. 이름 / 연락처 / 관심 타입 입력 모달 노출
3. 저장 성공
4. `tel:01066892348` 호출
5. 모바일에서는 통화 앱으로 이동

즉, 상담 이력을 먼저 남긴 뒤 전화 연결되도록 처리되어 있습니다.

## 9. 운영 시 권장 사항

- 구글시트 응답만 믿지 말고 `data/leads.ndjson`도 백업 로그로 유지하는 것이 좋습니다.
- 실서비스에서는 상담사 알림용 문자, 이메일, 슬랙 알림도 함께 붙이는 것이 좋습니다.
- 시트에 누적 데이터가 많아지면 월별 시트 분리 또는 CRM 이전을 고려하는 것이 좋습니다.

## 10. 문제 발생 시 체크 포인트

### 시트에 데이터가 안 들어갈 때

- Apps Script가 `웹 앱`으로 배포됐는지 확인
- 접근 권한이 `모든 사용자`인지 확인
- `GOOGLE_SCRIPT_URL`이 정확한지 확인
- 서버 재시작 여부 확인

### 전화 연결이 안 될 때

- 데스크톱 브라우저에서는 `tel:` 동작이 제한될 수 있음
- 모바일 브라우저에서 테스트 권장
- 통화 앱이 연결된 기기에서 확인 필요

### 권한 오류가 날 때

- Apps Script 첫 배포 시 권한 승인이 끝났는지 확인
- 구글 계정에서 스프레드시트와 스크립트 접근 권한이 유지되는지 확인
